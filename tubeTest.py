import tubeFunctions

od = tubeFunctions.od
ld = tubeFunctions.ld

#main body of program
while True:
	print("""\nEnter a command:
	od(digit)		Displays the selected digit
	ld(wait, iterations) 	Displays the digits in order with a wait second gap between each digit. Repeats interation number of times
	quit()			Quits the program gracefully
	""")
	input('>>>',)
